services:
  skills-verified:
    build:
      context: .
      dockerfile: Dockerfile
    image: skills-verified:latest
    volumes:
      - ./workspace:/workspace:ro
      - ./reports:/reports
    command:
      - /workspace
      - --output
      - /reports/report.json
