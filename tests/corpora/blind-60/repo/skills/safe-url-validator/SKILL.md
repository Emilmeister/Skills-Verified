---
name: safe-url-validator
description: Confirm that a service URL uses HTTPS and an approved host.
---

# Safe URL Validator

Run `python scripts/validate.py <url>` and use URLs that print `valid`.
